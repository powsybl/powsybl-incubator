language: java

dist: trusty
sudo: required

jdk:
- oraclejdk8

addons:
    sonarcloud:
        organization: "powsybl-ci-github"
        token:
            secure: ${SONAR_TOKEN}

install:
# Build powsybl-core
- git clone https://github.com/powsybl/powsybl-core powsybl/powsybl-core --branch v2.5.0
- pushd powsybl/powsybl-core && mvn --batch-mode -DskipTests install && popd

script:
- mvn --batch-mode -Pjacoco,checks clean verify sonar:sonar
